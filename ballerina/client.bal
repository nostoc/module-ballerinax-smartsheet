// AUTO-GENERATED FILE. DO NOT MODIFY.
// This file is auto-generated by the Ballerina OpenAPI tool.

// Copyright (c) 2025, WSO2 LLC. (http://www.wso2.com).
//
// WSO2 LLC. licenses this file to you under the Apache License,
// Version 2.0 (the "License"); you may not use this file except
// in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

import ballerina/data.jsondata;
import ballerina/data.xmldata;
import ballerina/http;
import ballerina/mime;

# Manages all of the common commerce functionality, and also includes customizations from installed AddOns. The implementing extension is called commercewebservices.
public isolated client class Client {
    final http:Client clientEp;
    # Gets invoked to initialize the `connector`.
    #
    # + config - The configurations to be used when initializing the `connector` 
    # + serviceUrl - URL of the target service 
    # + return - An error if connector initialization failed 
    public isolated function init(ConnectionConfig config, string serviceUrl = "http://localhost:9001/occ/v2") returns error? {
        http:ClientConfiguration httpClientConfig = {auth: config.auth, httpVersion: config.httpVersion, http1Settings: config.http1Settings, http2Settings: config.http2Settings, timeout: config.timeout, forwarded: config.forwarded, followRedirects: config.followRedirects, poolConfig: config.poolConfig, cache: config.cache, compression: config.compression, circuitBreaker: config.circuitBreaker, retryConfig: config.retryConfig, cookieConfig: config.cookieConfig, responseLimits: config.responseLimits, secureSocket: config.secureSocket, proxy: config.proxy, socketConfig: config.socketConfig, validation: config.validation, laxDataBinding: config.laxDataBinding};
        self.clientEp = check new (serviceUrl, httpClientConfig);
    }

    # Retrieves the base sites.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getBaseSites(map<string|string[]> headers = {}, *GetBaseSitesQueries queries) returns xml|error {
        string resourcePath = string `/basesites`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the public key to verify the access code.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getAccessCodePublicKey(string baseSiteId, map<string|string[]> headers = {}) returns SAPAccessCodePublicKey|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/accessCode/publicKey`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a base store.
    #
    # + baseSiteId - Base site identifier
    # + baseStoreUid - Base store identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getBaseStore(string baseSiteId, string baseStoreUid, map<string|string[]> headers = {}, *GetBaseStoreQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/basestores/${getEncodedUri(baseStoreUid)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a list of supported payment card types.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCardTypes(string baseSiteId, map<string|string[]> headers = {}, *GetCardTypesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/cardtypes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a list of catalogs.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCatalogs(string baseSiteId, map<string|string[]> headers = {}, *GetCatalogsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/catalogs`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a catalog.
    #
    # + baseSiteId - Base site identifier
    # + catalogId - Catalog identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCatalog(string baseSiteId, string catalogId, map<string|string[]> headers = {}, *GetCatalogQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/catalogs/${getEncodedUri(catalogId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves information about the catalog version.
    #
    # + baseSiteId - Base site identifier
    # + catalogId - Catalog identifier
    # + catalogVersionId - Catalog version identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCatalogVersion(string baseSiteId, string catalogId, string catalogVersionId, map<string|string[]> headers = {}, *GetCatalogVersionQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/catalogs/${getEncodedUri(catalogId)}/${getEncodedUri(catalogVersionId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves information about the category.
    #
    # + baseSiteId - Base site identifier
    # + catalogId - Catalog identifier
    # + catalogVersionId - Catalog version identifier
    # + categoryId - Category identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCategories(string baseSiteId, string catalogId, string catalogVersionId, string categoryId, map<string|string[]> headers = {}, *GetCategoriesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/catalogs/${getEncodedUri(catalogId)}/${getEncodedUri(catalogVersionId)}/categories/${getEncodedUri(categoryId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a list of products for a category.
    #
    # + baseSiteId - Base site identifier
    # + categoryId - Category identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getProductsByCategory(string baseSiteId, string categoryId, map<string|string[]> headers = {}, *GetProductsByCategoryQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/categories/${getEncodedUri(categoryId)}/products`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets a product configuration
    #
    # + baseSiteId - Base site identifier
    # + configId - Configuration identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getProductConfiguration(string baseSiteId, string configId, map<string|string[]> headers = {}, *GetProductConfigurationQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/ccpconfigurator/${getEncodedUri(configId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates a product configuration
    #
    # + baseSiteId - Base site identifier
    # + configId - Configuration identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function updateProductConfiguration(string baseSiteId, string configId, CCPConfiguration payload, map<string|string[]> headers = {}, *UpdateProductConfigurationQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/ccpconfigurator/${getEncodedUri(configId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Gets a product configuration overview
    #
    # + baseSiteId - Base site identifier
    # + configId - Configuration identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getProductConfigurationOverview(string baseSiteId, string configId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/ccpconfigurator/${getEncodedUri(configId)}/configurationOverview`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the product configuration overview
    #
    # + baseSiteId - Base site identifier
    # + configId - Configuration identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function updateProductConfigurationOverview(string baseSiteId, string configId, CCPConfigurationOverview payload, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/ccpconfigurator/${getEncodedUri(configId)}/configurationOverview`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Gets prices for a product configuration
    #
    # + baseSiteId - Base site identifier
    # + configId - Configuration identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getProductConfigurationPricing(string baseSiteId, string configId, map<string|string[]> headers = {}, *GetProductConfigurationPricingQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/ccpconfigurator/${getEncodedUri(configId)}/pricing`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets variants for a product configuration
    #
    # + baseSiteId - Base site identifier
    # + configId - Configuration identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getProductConfigurationVariantSearch(string baseSiteId, string configId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/ccpconfigurator/${getEncodedUri(configId)}/variants`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a list of districts for a city.
    #
    # + baseSiteId - Base site identifier
    # + cityId - City identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getDistricts(string baseSiteId, string cityId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/cities/${getEncodedUri(cityId)}/districts`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the component data.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ComponentIDListWsDTO 
    # 
    # # Deprecated
    @deprecated
    remote isolated function getComponentsByIds(string baseSiteId, map<string|string[]> headers = {}, *GetComponentsByIdsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/cms/components`;
        map<Encoding> queryParamEncoding = {"componentIds": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the component data.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ComponentIDListWsDTO 
    # 
    # # Deprecated
    @deprecated
    remote isolated function searchComponentsByIds(string baseSiteId, ComponentIDList payload, map<string|string[]> headers = {}, *SearchComponentsByIdsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/cms/components`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the component data.
    #
    # + baseSiteId - Base site identifier
    # + componentId - Component identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ComponentWsDTO 
    # 
    # # Deprecated
    @deprecated
    remote isolated function getComponentById(string baseSiteId, string componentId, map<string|string[]> headers = {}, *GetComponentByIdQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/cms/components/${getEncodedUri(componentId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the page data and the CMS content slots.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CMSPageWsDTO 
    # 
    # # Deprecated
    @deprecated
    remote isolated function getPage(string baseSiteId, map<string|string[]> headers = {}, *GetPageQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/cms/pages`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the page data and CMS content slots using the page identifier.
    #
    # + baseSiteId - Base site identifier
    # + pageId - Page Id
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CMSPageWsDTO 
    # 
    # # Deprecated
    @deprecated
    remote isolated function getPageById(string baseSiteId, string pageId, map<string|string[]> headers = {}, *GetPageByIdQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/cms/pages/${getEncodedUri(pageId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the page data.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CMSPageListWsDTO 
    # 
    # # Deprecated
    @deprecated
    remote isolated function getAllPages(string baseSiteId, map<string|string[]> headers = {}, *GetAllPagesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/cms/sitepages`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves active cost centers.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getActiveCostCenters(string baseSiteId, map<string|string[]> headers = {}, *GetActiveCostCentersQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/costcenters`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a cost center.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createCostCenter(string baseSiteId, B2BCostCenter payload, map<string|string[]> headers = {}, *CreateCostCenterQueries queries) returns B2BCostCenter|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/costcenters`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the cost center.
    #
    # + baseSiteId - Base site identifier
    # + costCenterCode - Cost center identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCostCenter(string baseSiteId, string costCenterCode, map<string|string[]> headers = {}, *GetCostCenterQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/costcenters/${getEncodedUri(costCenterCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the cost center.
    #
    # + baseSiteId - Base site identifier
    # + costCenterCode - Cost center identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function updateCostCenter(string baseSiteId, string costCenterCode, B2BCostCenter payload, map<string|string[]> headers = {}, *UpdateCostCenterQueries queries) returns B2BCostCenter|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/costcenters/${getEncodedUri(costCenterCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves all budgets and certain budgets associated with the specified cost center.
    #
    # + baseSiteId - Base site identifier
    # + costCenterCode - Cost center identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getBudgetsForCostCenter(string baseSiteId, string costCenterCode, map<string|string[]> headers = {}, *GetBudgetsForCostCenterQueries queries) returns BudgetList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/costcenters/${getEncodedUri(costCenterCode)}/budgets`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a budget for the cost center.
    #
    # + baseSiteId - Base site identifier
    # + costCenterCode - Cost center to which the budget will be added
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function doAddBudgetToCostCenter(string baseSiteId, string costCenterCode, map<string|string[]> headers = {}, *DoAddBudgetToCostCenterQueries queries) returns B2BSelectionData|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/costcenters/${getEncodedUri(costCenterCode)}/budgets`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the budget from a cost center.
    #
    # + baseSiteId - Base site identifier
    # + budgetCode - Budget that will be removed from a specific cost center
    # + costCenterCode - Cost center from which the budget will be removed
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function removeBudgetFromCostCenter(string baseSiteId, string budgetCode, string costCenterCode, map<string|string[]> headers = {}, *RemoveBudgetFromCostCenterQueries queries) returns B2BSelectionData|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/costcenters/${getEncodedUri(costCenterCode)}/budgets/${getEncodedUri(budgetCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves the cost centers.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCostCenters(string baseSiteId, map<string|string[]> headers = {}, *GetCostCentersQueries queries) returns B2BCostCenterList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/costcentersall`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a list of countries.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCountries(string baseSiteId, map<string|string[]> headers = {}, *GetCountriesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/countries`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a list of regions.
    #
    # + baseSiteId - Base site identifier
    # + countyIsoCode - An ISO code for a country
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCountryRegions(string baseSiteId, string countyIsoCode, map<string|string[]> headers = {}, *GetCountryRegionsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/countries/${getEncodedUri(countyIsoCode)}/regions`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the CPQ attribute
    #
    # + attributeCode - Standard attribute code
    # + baseSiteId - Base site identifier
    # + configurationId - Configuration identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function updateCpqAttribute(int:Signed32 attributeCode, string baseSiteId, string configurationId, SAPCPQConfigurationChangeAttributeValue payload, map<string|string[]> headers = {}, *UpdateCpqAttributeQueries queries) returns SAPCPQConfiguration|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/cpqconfigurator/${getEncodedUri(configurationId)}/attributes/${getEncodedUri(attributeCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Updates the quantity of the CPQ attribute value
    #
    # + attributeCode - Standard attribute code
    # + attributeValueId - Attribute value identifier
    # + baseSiteId - Base site identifier
    # + configurationId - Configuration identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function updateCpqAttributeValueQuantity(int:Signed32 attributeCode, int:Signed32 attributeValueId, string baseSiteId, string configurationId, SAPCPQConfigurationChangeAttributeValueQuantity payload, map<string|string[]> headers = {}, *UpdateCpqAttributeValueQuantityQueries queries) returns SAPCPQConfiguration|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/cpqconfigurator/${getEncodedUri(configurationId)}/attributes/${getEncodedUri(attributeCode)}/values/${getEncodedUri(attributeValueId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves a configuration tab.
    #
    # + baseSiteId - Base site identifier
    # + configurationId - Configuration identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function readCpqConfiguration(string baseSiteId, string configurationId, map<string|string[]> headers = {}, *ReadCpqConfigurationQueries queries) returns SAPCPQConfiguration|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/cpqconfigurator/${getEncodedUri(configurationId)}/configuration`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves CPQ configuration overview.
    #
    # + baseSiteId - Base site identifier
    # + configurationId - Configuration identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function readCpqConfigurationOverview(string baseSiteId, string configurationId, map<string|string[]> headers = {}) returns SAPCPQConfiguration|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/cpqconfigurator/${getEncodedUri(configurationId)}/configurationOverview`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a list of available currencies.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCurrencies(string baseSiteId, map<string|string[]> headers = {}, *GetCurrenciesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/currencies`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the subgroups of a customer group.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCustomerGroups(string baseSiteId, map<string|string[]> headers = {}, *GetCustomerGroupsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/customergroups`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a new customer group.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createCustomerGroup(string baseSiteId, UserGroup payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/customergroups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves a customer group.
    #
    # + baseSiteId - Base site identifier
    # + groupId - Group identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCustomerGroup(string baseSiteId, string groupId, map<string|string[]> headers = {}, *GetCustomerGroupQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/customergroups/${getEncodedUri(groupId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Sets members for a user group.
    #
    # + baseSiteId - Base site identifier
    # + groupId - Group identifier. 
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function replaceUsersForCustomerGroup(string baseSiteId, string groupId, MemberList payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/customergroups/${getEncodedUri(groupId)}/members`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Assigns members to a customer group.
    #
    # + baseSiteId - Base site identifier
    # + groupId - Group identifier. 
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function updateCustomerGroupWithUsers(string baseSiteId, string groupId, MemberList payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/customergroups/${getEncodedUri(groupId)}/members`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Deletes the customer from a customer group.
    #
    # + baseSiteId - Base site identifier
    # + groupId - Group identifier
    # + userId - User identifier or one of the literals: ‘current’ for currently authenticated user, ‘anonymous’ for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeUsersFromCustomerGroup(string baseSiteId, string groupId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/customergroups/${getEncodedUri(groupId)}/members/${getEncodedUri(userId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves a list of shipping countries.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    # 
    # # Deprecated
    @deprecated
    remote isolated function getDeliveryCountries(string baseSiteId, map<string|string[]> headers = {}, *GetDeliveryCountriesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/deliverycountries`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a list of exported products.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getExportedProducts(string baseSiteId, map<string|string[]> headers = {}, *GetExportedProductsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/export/products`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a list of orders with status updates.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrderStatusFeed(string baseSiteId, map<string|string[]> headers = {}, *GetOrderStatusFeedQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/feeds/orders/statusfeed`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a token to restore a forgotten password.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Accepted 
    # 
    # # Deprecated
    @deprecated
    remote isolated function doRestorePassword(string baseSiteId, map<string|string[]> headers = {}, *DoRestorePasswordQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/forgottenpasswordtokens`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Validates the decision of the merchant.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function doHandleMerchantCallback(string baseSiteId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/integration/merchant_callback`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Validates the decision of the merchant for a SOP payment related to a cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged in user, cart guid for anonymous user, 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function doHandleCartMerchantCallback(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/integration/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/payment/sop/response`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves a list of available languages.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getLanguages(string baseSiteId, map<string|string[]> headers = {}, *GetLanguagesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/languages`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Sends a message.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function sendMessage(string baseSiteId, ConversationMessageList payload, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/messagecenter/im/conversations`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the unassigned or open conversations.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getConversationsForAgent(string baseSiteId, map<string|string[]> headers = {}, *GetConversationsForAgentQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/messagecenter/im/conversations/agentconversations`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the conversations.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getConversationsForCustomer(string baseSiteId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/messagecenter/im/conversations/customerconversations`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Closes an open conversation.
    #
    # + baseSiteId - Base site identifier
    # + conversationId - Conversation identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function closeConversation(string baseSiteId, string conversationId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/messagecenter/im/conversations/${getEncodedUri(conversationId)}/close`;
        http:Request request = new;
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves the messages of a specific conversation.
    #
    # + baseSiteId - Base site identifier
    # + conversationId - Conversation identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getMessagesForConversation(string baseSiteId, string conversationId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/messagecenter/im/conversations/${getEncodedUri(conversationId)}/messages`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Picks an unassigned conversation.
    #
    # + baseSiteId - Base site identifier
    # + conversationId - Conversation identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function pickConversation(string baseSiteId, string conversationId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/messagecenter/im/conversations/${getEncodedUri(conversationId)}/pick`;
        http:Request request = new;
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves the order approval permission types.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrderApprovalPermissionTypes(string baseSiteId, map<string|string[]> headers = {}, *GetOrderApprovalPermissionTypesQueries queries) returns OrderApprovalPermissionTypeList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orderApprovalPermissionTypes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves all order forms.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function retrieveAllOrderForms(string baseSiteId, map<string|string[]> headers = {}, *RetrieveAllOrderFormsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orderforms`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an order form.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createAnOrderForm(string baseSiteId, OrderForm payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orderforms`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves an order form.
    #
    # + baseSiteId - Base site identifier
    # + orderFormCode - The order form identifier. Example: 0000000
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function retrieveAnOrderForm(string baseSiteId, string orderFormCode, map<string|string[]> headers = {}, *RetrieveAnOrderFormQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orderforms/${getEncodedUri(orderFormCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates an order form.
    #
    # + baseSiteId - Base site identifier
    # + orderFormCode - The order form identifier. Example: 0000000
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function updateAnOrderForm(string baseSiteId, string orderFormCode, OrderForm payload, map<string|string[]> headers = {}, *UpdateAnOrderFormQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orderforms/${getEncodedUri(orderFormCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes an order form.
    #
    # + baseSiteId - Base site identifier
    # + orderFormCode - The order form identifier. Example: 0000000
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteAnOrderForm(string baseSiteId, string orderFormCode, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orderforms/${getEncodedUri(orderFormCode)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Adds products in an order form to the current cart.
    #
    # + baseSiteId - Base site identifier
    # + orderFormCode - The order form identifier. Example: 0000000
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - No Content 
    remote isolated function addOrderFormToCart(string baseSiteId, string orderFormCode, map<string|string[]> headers = {}, *AddOrderFormToCartQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orderforms/${getEncodedUri(orderFormCode)}/cart`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the order.
    #
    # + baseSiteId - Base site identifier
    # + code - Order GUID or order code
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrder(string baseSiteId, string code, map<string|string[]> headers = {}, *GetOrderQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orders/${getEncodedUri(code)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves product details.
    #
    # + baseSiteId - Base site identifier
    # + productCode - Product identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgProduct(string baseSiteId, string productCode, map<string|string[]> headers = {}, *GetOrgProductQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orgProducts/${getEncodedUri(productCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a registration request for a B2B customer.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createRegistrationRequest(string baseSiteId, OrgUserRegistrationData payload, CreateRegistrationRequestHeaders headers = {}, *CreateRegistrationRequestQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orgUsers`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieves a B2B customer profile.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgUser(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetOrgUserQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orgUsers/${getEncodedUri(userId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a cart from a previous order.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createCartFromOrder(string baseSiteId, string userId, map<string|string[]> headers = {}, *CreateCartFromOrderQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orgUsers/${getEncodedUri(userId)}/cartFromOrder`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Updates the delivery address for a cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function replaceOrgCartDeliveryAddress(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *ReplaceOrgCartDeliveryAddressQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orgUsers/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/addresses/delivery`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Updates the quantity of the specified products in a cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function replaceOrgCartEntries(string baseSiteId, string cartId, string userId, OrderEntryList payload, map<string|string[]> headers = {}, *ReplaceOrgCartEntriesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orgUsers/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates additional quantity of a product in the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function doAddOrgCartEntries(string baseSiteId, string cartId, string userId, OrderEntryList payload, map<string|string[]> headers = {}, *DoAddOrgCartEntriesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orgUsers/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Creates a B2B order.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function placeOrgOrder(string baseSiteId, string userId, map<string|string[]> headers = {}, *PlaceOrgOrderQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orgUsers/${getEncodedUri(userId)}/orders`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the order history for the customer's organization branch.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getUserBranchOrderHistory(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetUserBranchOrderHistoryQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orgUsers/${getEncodedUri(userId)}/orgUnits/orders`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the order from the customer's organization branch.
    #
    # + baseSiteId - Base site identifier
    # + code - Order code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getBranchOrder(string baseSiteId, string code, string userId, map<string|string[]> headers = {}, *GetBranchOrderQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orgUsers/${getEncodedUri(userId)}/orgUnits/orders/${getEncodedUri(code)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates replenishment orders.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createReplenishmentOrder(string baseSiteId, string userId, ScheduleReplenishmentForm payload, map<string|string[]> headers = {}, *CreateReplenishmentOrderQueries queries) returns ReplenishmentOrder|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/orgUsers/${getEncodedUri(userId)}/replenishmentOrders`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Creates and sends a token to restore a forgotten password.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function sendPasswordRestoreToken(string baseSiteId, PasswordRestoreTokenInput payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/passwordRestoreToken`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the available payment modes.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getPaymentModes(string baseSiteId, map<string|string[]> headers = {}, *GetPaymentModesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/paymentmodes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the available payment types.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getPaymentTypes(string baseSiteId, map<string|string[]> headers = {}, *GetPaymentTypesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/paymenttypes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves availability information for a list of products.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getProductAvailability(string baseSiteId, map<string|string[]> headers = {}, *GetProductAvailabilityQueries queries) returns SAPAvailability|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/productAvailabilities`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves products that were added to the express update feed.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getExpressUpdateProducts(string baseSiteId, map<string|string[]> headers = {}, *GetExpressUpdateProductsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/expressupdate`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a list of products.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getProducts(string baseSiteId, map<string|string[]> headers = {}, *GetProductsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/search`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the total number of products.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function countProducts(string baseSiteId, map<string|string[]> headers = {}, *CountProductsQueries queries) returns http:Response|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/search`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->head(resourcePath, headers);
    }

    # Retrieves the available suggestions.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getSuggestions(string baseSiteId, map<string|string[]> headers = {}, *GetSuggestionsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/suggestions`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves product details.
    #
    # + baseSiteId - Base site identifier
    # + productCode - Product identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getProduct(string baseSiteId, string productCode, map<string|string[]> headers = {}, *GetProductQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/${getEncodedUri(productCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get textfield configuration
    #
    # + baseSiteId - Base site identifier
    # + productCode - Product identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getConfigurationByProductCode(string baseSiteId, string productCode, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/${getEncodedUri(productCode)}/configurator/textfield`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets the default product configuration for a complex product
    #
    # + baseSiteId - Base site identifier
    # + productCode - Product code
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getDefaultProductConfiguration(string baseSiteId, string productCode, map<string|string[]> headers = {}, *GetDefaultProductConfigurationQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/${getEncodedUri(productCode)}/configurators/ccpconfigurator`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a configuration of the CPQ product.
    #
    # + baseSiteId - Base site identifier
    # + productCode - Product code
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function createCpqConfiguration(string baseSiteId, string productCode, map<string|string[]> headers = {}) returns SAPCPQConfiguration|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/${getEncodedUri(productCode)}/configurators/cpqconfigurator`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the product references.
    #
    # + baseSiteId - Base site identifier
    # + productCode - Product identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getProductReferences(string baseSiteId, string productCode, map<string|string[]> headers = {}, *GetProductReferencesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/${getEncodedUri(productCode)}/references`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the reviews of a product.
    #
    # + baseSiteId - Base site identifier
    # + productCode - Product identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getProductReviews(string baseSiteId, string productCode, map<string|string[]> headers = {}, *GetProductReviewsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/${getEncodedUri(productCode)}/reviews`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a customer review as an anonymous user.
    #
    # + baseSiteId - Base site identifier
    # + productCode - Product identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createProductReview(string baseSiteId, string productCode, Review payload, map<string|string[]> headers = {}, *CreateProductReviewQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/${getEncodedUri(productCode)}/reviews`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the stock level of a product.
    #
    # + baseSiteId - Base site identifier
    # + productCode - Product identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getLocationProductStock(string baseSiteId, string productCode, map<string|string[]> headers = {}, *GetLocationProductStockQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/${getEncodedUri(productCode)}/stock`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the total number of stock levels of a product.
    #
    # + baseSiteId - Base site identifier
    # + productCode - Product identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function countProductStockByLocation(string baseSiteId, string productCode, map<string|string[]> headers = {}, *CountProductStockByLocationQueries queries) returns http:Response|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/${getEncodedUri(productCode)}/stock`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->head(resourcePath, headers);
    }

    # Retrieves the stock level of a product.
    #
    # + baseSiteId - Base site identifier
    # + productCode - Product identifier
    # + storeName - Store identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getStoreProductStock(string baseSiteId, string productCode, string storeName, map<string|string[]> headers = {}, *GetStoreProductStockQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/products/${getEncodedUri(productCode)}/stock/${getEncodedUri(storeName)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the promotions.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getPromotions(string baseSiteId, map<string|string[]> headers = {}, *GetPromotionsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/promotions`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the promotion.
    #
    # + baseSiteId - Base site identifier
    # + code - Promotion identifier (code)
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getPromotion(string baseSiteId, string code, map<string|string[]> headers = {}, *GetPromotionQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/promotions/${getEncodedUri(code)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Handles a purchase order request from the PunchOut provider.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function createPunchOutPurchaseOrderRequest(string baseSiteId, CXML payload, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/punchout/cxml/order`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Creates a PunchOut profile request.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function createPunchOutProfileRequest(string baseSiteId, CreatePunchOutProfileRequestHeaders headers, CXML payload) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/punchout/cxml/profile`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Creates a new PunchOut session.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function createPunchOutSetUpRequest(string baseSiteId, CXML payload, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/punchout/cxml/setup`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves information about PunchOut session.
    #
    # + baseSiteId - Base site identifier
    # + sid - PunchOut session identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getPunchOutSessionInfo(string baseSiteId, string sid, map<string|string[]> headers = {}, *GetPunchOutSessionInfoQueries queries) returns PunchOutSessionInfo|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/punchout/sessions/${getEncodedUri(sid)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the order message and URL for the procurement system.
    #
    # + baseSiteId - Base site identifier
    # + sid - PunchOut session identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getRequisitionFormData(string baseSiteId, string sid, map<string|string[]> headers = {}, *GetRequisitionFormDataQueries queries) returns RequisitionFormData|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/punchout/sessions/${getEncodedUri(sid)}/requisition`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a list of cities for a region.
    #
    # + baseSiteId - Base site identifier
    # + regionId - Region identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getCities(string baseSiteId, string regionId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/regions/${getEncodedUri(regionId)}/cities`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Resets a forgotten password.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + return - Accepted 
    remote isolated function doResetPassword(string baseSiteId, ResetPassword payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/resetpassword`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the store locations.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getStoreLocations(string baseSiteId, map<string|string[]> headers = {}, *GetStoreLocationsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/stores`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a header that indicates the number of store locations.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function countStoreLocations(string baseSiteId, map<string|string[]> headers = {}, *CountStoreLocationsQueries queries) returns http:Response|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/stores`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->head(resourcePath, headers);
    }

    # Retrieves the store locations in a country.
    #
    # + baseSiteId - Base site identifier
    # + countryIso - Country ISO code
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getStoresByCountry(string baseSiteId, string countryIso, map<string|string[]> headers = {}, *GetStoresByCountryQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/stores/country/${getEncodedUri(countryIso)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the store locations.
    #
    # + baseSiteId - Base site identifier
    # + countryIso - Country ISO code
    # + regionIso - Region ISO code
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getStoresByCountryAndRegion(string baseSiteId, string countryIso, string regionIso, map<string|string[]> headers = {}, *GetStoresByCountryAndRegionQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/stores/country/${getEncodedUri(countryIso)}/region/${getEncodedUri(regionIso)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves store counts.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getLocationCounts(string baseSiteId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/stores/storescounts`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a store location.
    #
    # + baseSiteId - Base site identifier
    # + storeId - Store identifier (currently store name)
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getStoreLocation(string baseSiteId, string storeId, map<string|string[]> headers = {}, *GetStoreLocationQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/stores/${getEncodedUri(storeId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the ticket categories.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getTicketCategories(string baseSiteId, map<string|string[]> headers = {}, *GetTicketCategoriesQueries queries) returns TicketCategoryList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/ticketCategories`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the localized titles.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getTitles(string baseSiteId, map<string|string[]> headers = {}, *GetTitlesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/titles`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    #  Registers a customer.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createUser(string baseSiteId, UserSignUp payload, CreateUserHeaders headers = {}, *CreateUserQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieves the customer profile.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getUser(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetUserQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the customer profile.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function replaceUser(string baseSiteId, string userId, User payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes the customer profile.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeUser(string baseSiteId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates customer profile
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function updateUser(string baseSiteId, string userId, User payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves access data.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getAccessToConfigurationEngine(string baseSiteId, string userId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/access/cpqconfigurator`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the addresses of a customer.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - List of customer's addresses 
    remote isolated function getAddresses(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetAddressesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/addresses`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a new address.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createAddress(string baseSiteId, string userId, Address payload, map<string|string[]> headers = {}, *CreateAddressQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/addresses`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Verifies the address.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function validateAddress(string baseSiteId, string userId, Address payload, map<string|string[]> headers = {}, *ValidateAddressQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/addresses/verification`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the address details of a customer.
    #
    # + addressId - Address identifier
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getAddress(string addressId, string baseSiteId, string userId, map<string|string[]> headers = {}, *GetAddressQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/addresses/${getEncodedUri(addressId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the address of a customer.
    #
    # + addressId - Address identifier
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function replaceAddress(string addressId, string baseSiteId, string userId, Address payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/addresses/${getEncodedUri(addressId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes the address of a customer.
    #
    # + addressId - Address identifier
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeAddress(string addressId, string baseSiteId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/addresses/${getEncodedUri(addressId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates the address
    #
    # + addressId - Address identifier
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function updateAddress(string addressId, string baseSiteId, string userId, Address payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/addresses/${getEncodedUri(addressId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves the available organizational unit nodes.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getAvailableOrgUnitNodes(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetAvailableOrgUnitNodesQueries queries) returns B2BUnitNodeList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/availableOrgUnitNodes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the list of budgets.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getBudgets(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetBudgetsQueries queries) returns BudgetList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/budgets`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a new budget.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createBudget(string baseSiteId, string userId, Budget payload, map<string|string[]> headers = {}, *CreateBudgetQueries queries) returns Budget|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/budgets`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the details of a budget.
    #
    # + baseSiteId - Base site identifier
    # + budgetCode - Budget GUID or budget code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getBudget(string baseSiteId, string budgetCode, string userId, map<string|string[]> headers = {}, *GetBudgetQueries queries) returns Budget|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/budgets/${getEncodedUri(budgetCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the budget.
    #
    # + baseSiteId - Base site identifier
    # + budgetCode - Budget GUID or budget code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function updateBudget(string baseSiteId, string budgetCode, string userId, Budget payload, map<string|string[]> headers = {}, *UpdateBudgetQueries queries) returns Budget|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/budgets/${getEncodedUri(budgetCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves the carts of a customer.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCarts(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetCartsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates or restore a cart for a user.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createCart(string baseSiteId, string userId, map<string|string[]> headers = {}, *CreateCartQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets the current cart.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCurrentOrgCart(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetCurrentOrgCartQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/current`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCart(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *GetCartQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeCart(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Generates an access code for the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getCartIdAccessCode(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}) returns SAPAccessCode|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/accessCode`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Create or update the billing address of the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function updateBillingAddress(string baseSiteId, string cartId, string userId, Address payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/addresses/billing`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Updates the delivery address for a cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function replaceCartDeliveryAddress(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *ReplaceCartDeliveryAddressQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/addresses/delivery`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates a delivery address for the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createCartDeliveryAddress(string baseSiteId, string cartId, string userId, Address payload, map<string|string[]> headers = {}, *CreateCartDeliveryAddressQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/addresses/delivery`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the delivery address.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeCartDeliveryAddress(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/addresses/delivery`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Assigns a voucher to the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function applyCartVoucher(string baseSiteId, string cartId, string userId, SAPVoucherRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/applyVoucher`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Creates a bundle in a cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createBundle(string baseSiteId, string cartId, string userId, BundleStarter payload, map<string|string[]> headers = {}) returns CartModification|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/bundles`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Creates a clone of a saved cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    # 
    # # Deprecated
    @deprecated
    remote isolated function doCartClone(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *DoCartCloneQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/clonesavedcart`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves a list of pickup options.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getConsolidatedPickupLocations(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *GetConsolidatedPickupLocationsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/consolidate`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a consolidated pickup location.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function createConsolidatedPickupLocation(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *CreateConsolidatedPickupLocationQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/consolidate`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Creates a clone of a saved cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function copySavedCart(string baseSiteId, string cartId, string userId, SAPSavedCartRequest payload, map<string|string[]> headers = {}, *CopySavedCartQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/copySavedCart`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Updates the cost center for the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function replaceOrgCartCostCenter(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *ReplaceOrgCartCostCenterQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/costcenter`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Retrieves the delivery mode for the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCartDeliveryMode(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *GetCartDeliveryModeQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/deliverymode`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the delivery mode for the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function replaceCartDeliveryMode(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *ReplaceCartDeliveryModeQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/deliverymode`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes the delivery mode from the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeCartDeliveryMode(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/deliverymode`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves the delivery modes for the current store and delivery address.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCartDeliveryModes(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *GetCartDeliveryModesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/deliverymodes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Assigns an email address to the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    # 
    # # Deprecated
    @deprecated
    remote isolated function replaceCartGuestUser(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *ReplaceCartGuestUserQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/email`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Retrieves the cart entries.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCartEntries(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *GetCartEntriesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Assigns a product to the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function createCartEntry(string baseSiteId, string cartId, string userId, OrderEntry payload, map<string|string[]> headers = {}, *CreateCartEntryQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Adds a product configuration to the cart
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createCartEntryConfiguration(string baseSiteId, string cartId, string userId, CCPOrderEntry payload, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/ccpconfigurator`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Adds a product to the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function addCartEntry(string baseSiteId, string cartId, string userId, OrderEntry payload, map<string|string[]> headers = {}, *AddCartEntryQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/configurator/textfield`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Adds a configurable product to the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createCartEntryCPQConfiguration(string baseSiteId, string cartId, string userId, CPQOrderEntry payload, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/cpqconfigurator`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the details of a cart entry.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCartEntry(string baseSiteId, string cartId, int entryNumber, string userId, map<string|string[]> headers = {}, *GetCartEntryQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/${getEncodedUri(entryNumber)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the details of a cart entry.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function replaceCartEntry(string baseSiteId, string cartId, int entryNumber, string userId, OrderEntry payload, map<string|string[]> headers = {}, *ReplaceCartEntryQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/${getEncodedUri(entryNumber)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes the cart entry.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeCartEntry(string baseSiteId, string cartId, int entryNumber, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/${getEncodedUri(entryNumber)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates the details of a cart entry.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function updateCartEntry(string baseSiteId, string cartId, int entryNumber, string userId, OrderEntry payload, map<string|string[]> headers = {}, *UpdateCartEntryQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/${getEncodedUri(entryNumber)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Gets the configuration of a cart entry
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCartEntryConfiguration(string baseSiteId, string cartId, int:Signed32 entryNumber, string userId, map<string|string[]> headers = {}, *GetCartEntryConfigurationQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/${getEncodedUri(entryNumber)}/ccpconfigurator`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the configuration of a cart entry
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function replaceCartEntryConfiguration(string baseSiteId, string cartId, int:Signed32 entryNumber, string userId, CCPOrderEntry payload, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/${getEncodedUri(entryNumber)}/ccpconfigurator`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets a product configuration overview of an saved cart entry
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getConfigurationOverviewForSavedCart(string baseSiteId, string cartId, int:Signed32 entryNumber, string userId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/${getEncodedUri(entryNumber)}/ccpconfigurator/configurationOverview`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Get configuration of cart entry.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + entryNumber - Cart entry number
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getConfigurationInEntry(string baseSiteId, string cartId, int:Signed32 entryNumber, string userId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/${getEncodedUri(entryNumber)}/configurator/textfield`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Update configuration of cart entry.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + entryNumber - Cart entry number
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getConfigurationInEntry1(string baseSiteId, string cartId, int:Signed32 entryNumber, string userId, ConfigurationInfoList payload, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/${getEncodedUri(entryNumber)}/configurator/textfield`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Updates the configuration.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + entryNumber - Each entry in the cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function replaceCartEntryCPQConfiguration(string baseSiteId, string cartId, int:Signed32 entryNumber, string userId, CPQOrderEntry payload, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/${getEncodedUri(entryNumber)}/cpqconfigurator`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Retrieves the CPQ configuration of a cart entry.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + entryNumber - Each entry in the cart has an entry number. Cart entries are numbered in ascending order
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function readCpqConfigurationForCartEntry(string baseSiteId, string cartId, int:Signed32 entryNumber, string userId, map<string|string[]> headers = {}) returns SAPCPQConfiguration|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entries/${getEncodedUri(entryNumber)}/cpqconfigurator/configuration`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Assigns a product to a cart entry group.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + entryGroupNumber - Each entry group in a cart has a specific entry group number. Entry group numbers are integers starting at one. They are defined in ascending order
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function addToCartEntryGroup(string baseSiteId, string cartId, int:Signed32 entryGroupNumber, string userId, OrderEntry payload, map<string|string[]> headers = {}, *AddToCartEntryGroupQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entrygroups/${getEncodedUri(entryGroupNumber)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes an entry group.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + entryGroupNumber - Each entry group in a cart has a specific entry group number. Entry group numbers are integers starting at one. They are defined in ascending order
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeCartEntryGroup(string baseSiteId, string cartId, int:Signed32 entryGroupNumber, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entrygroups/${getEncodedUri(entryGroupNumber)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves products for an entry group.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + entryGroupNumber - Each entry group in a cart has a specific entry group number. Entry group numbers are integers starting at one. They are defined in ascending order
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getAvailableProducts(string baseSiteId, string cartId, int:Signed32 entryGroupNumber, string userId, map<string|string[]> headers = {}, *GetAvailableProductsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/entrygroups/${getEncodedUri(entryGroupNumber)}/allowedProductsSearch`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the cart by flagging it for deletion.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function doUpdateFlagForDeletion(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *DoUpdateFlagForDeletionQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/flagForDeletion`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Creates a guest user for the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createCartGuestUser(string baseSiteId, string cartId, string userId, SAPGuestUserRequest payload, map<string|string[]> headers = {}, *CreateCartGuestUserQueries queries) returns SAPGuestUserRequest|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/guestuser`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Updates profile for current cart guest user.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function updateCurrentUserProfile(string baseSiteId, string cartId, string userId, SAPGuestUserRequest payload, map<string|string[]> headers = {}, *UpdateCurrentUserProfileQueries queries) returns SAPGuestUserRequest|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/guestuser`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Requests information to create a subscription.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function createDigitalPaymentRequest(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *CreateDigitalPaymentRequestQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/payment/digitalPayments/request`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Creates the payment details from the response of the digital payments system.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function doHandleDigitalPaymentResponse(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *DoHandleDigitalPaymentResponseQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/payment/digitalPayments/response`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the information required to create a subscription.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged in user, cart guid for anonymous user, 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getSopPaymentRequestDetails(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *GetSopPaymentRequestDetailsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/payment/sop/request`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the create subscription response from the payment provider.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getSopPaymentResponse(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *GetSopPaymentResponseQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/payment/sop/response`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates the SOP payment details.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function doHandleSopPaymentResponse(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *DoHandleSopPaymentResponseQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/payment/sop/response`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the response from the payment provider.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeSopPaymentResponse(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/payment/sop/response`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Set the payment option for the cart.
    #
    # + baseSiteId - The base site identifier
    # + cartId - Cart identifier: cart code for logged-in user
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function updateSapPaymentOption(string baseSiteId, string cartId, string userId, SAPPaymentOptionRequest payload, map<string|string[]> headers = {}, *UpdateSapPaymentOptionQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/paymentOption`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Sets credit card payment details for the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function replaceCartPaymentDetails(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *ReplaceCartPaymentDetailsQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/paymentdetails`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates details of a new credit card payment for the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createCartPaymentDetails(string baseSiteId, string cartId, string userId, PaymentDetails payload, map<string|string[]> headers = {}, *CreateCartPaymentDetailsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/paymentdetails`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Updates the payment type for the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function replaceOrgCartPaymentType(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *ReplaceOrgCartPaymentTypeQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/paymenttype`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Retrieves the cart promotions.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCartPromotions(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *GetCartPromotionsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/promotions`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Assigns a promotion to the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function doApplyCartPromotion(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *DoApplyCartPromotionQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/promotions`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves information about the promotion.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + promotionId - Promotion identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCartPromotion(string baseSiteId, string cartId, string promotionId, string userId, map<string|string[]> headers = {}, *GetCartPromotionQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/promotions/${getEncodedUri(promotionId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes the promotion.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + promotionId - Promotion identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeCartPromotion(string baseSiteId, string cartId, string promotionId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/promotions/${getEncodedUri(promotionId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Removes a voucher defined for the current cart. This endpoint is added in the 2211.28 update.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function doCartVoucherRemoval(string baseSiteId, string cartId, string userId, SAPVoucherRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/removeVoucher`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Sets the requested retrieval date for a cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function setCartRequestedRetrievalDate(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *SetCartRequestedRetrievalDateQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/requestedretrievaldate`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Restores a saved cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function doUpdateSavedCart(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *DoUpdateSavedCartQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/restoresavedcart`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Updates a cart to save it.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    # 
    # # Deprecated
    @deprecated
    remote isolated function doSaveCart(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *DoSaveCartQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/save`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Updates a cart to save it.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function doCartSave(string baseSiteId, string cartId, string userId, SAPSavedCartRequest payload, map<string|string[]> headers = {}, *DoCartSaveQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/savedCart`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves the saved cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getSavedCart(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *GetSavedCartQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/savedcart`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the scheduled service order date for the products in a cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged in user, cart guid for anonymous user, 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function updateCartServiceScheduleSlot(string baseSiteId, string cartId, string userId, ServiceScheduleSlot payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/serviceOrder/serviceScheduleSlot`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Assigns an email address to the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function setGuestUserEmail(string baseSiteId, string cartId, string userId, SAPGuestUserRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/setEmail`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Validates the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function validateCart(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *ValidateCartQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/validate`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves a list of vouchers.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCartVouchers(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *GetCartVouchersQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/vouchers`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Assigns a voucher to the cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    # 
    # # Deprecated
    @deprecated
    remote isolated function doApplyCartVoucher(string baseSiteId, string cartId, string userId, map<string|string[]> headers = {}, *DoApplyCartVoucherQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/vouchers`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes a voucher defined for the current cart.
    #
    # + baseSiteId - Base site identifier
    # + cartId - Cart identifier: cart code for logged-in user, cart GUID for anonymous user, or 'current' for the last modified cart
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + voucherId - Voucher identifier (code)
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    # 
    # # Deprecated
    @deprecated
    remote isolated function removeCartVoucher(string baseSiteId, string cartId, string userId, string voucherId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/carts/${getEncodedUri(cartId)}/vouchers/${getEncodedUri(voucherId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves the component data.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ComponentIDListWsDTO 
    remote isolated function getComponentsByIdsAndUser(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetComponentsByIdsAndUserQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/cms/components`;
        map<Encoding> queryParamEncoding = {"componentIds": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the component data.
    #
    # + baseSiteId - Base site identifier
    # + componentId - Component identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ComponentWsDTO 
    remote isolated function getComponentByIdAndUser(string baseSiteId, string componentId, string userId, map<string|string[]> headers = {}, *GetComponentByIdAndUserQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/cms/components/${getEncodedUri(componentId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the page data and the CMS content slots.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CMSPageWsDTO 
    remote isolated function getPageWithUser(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetPageWithUserQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/cms/pages`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the page data and CMS content slots using the page identifier.
    #
    # + baseSiteId - Base site identifier
    # + pageId - Page Id
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CMSPageWsDTO 
    remote isolated function getPageByIdAndUser(string baseSiteId, string pageId, string userId, map<string|string[]> headers = {}, *GetPageByIdAndUserQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/cms/pages/${getEncodedUri(pageId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the page data.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CMSPageListWsDTO 
    remote isolated function getAllPagesWithUser(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetAllPagesWithUserQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/cms/sitepages`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates consent.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function doGiveConsent(string baseSiteId, string userId, map<string|string[]> headers = {}, *DoGiveConsentQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/consents`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the user consent.
    #
    # + baseSiteId - Base site identifier
    # + consentCode - Consent code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeConsent(string baseSiteId, string consentCode, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/consents/${getEncodedUri(consentCode)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves the consents.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getConsentTemplates(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetConsentTemplatesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/consenttemplates`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the consent.
    #
    # + baseSiteId - Base site identifier
    # + consentTemplateId - Consent template identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getConsentTemplate(string baseSiteId, string consentTemplateId, string userId, map<string|string[]> headers = {}, *GetConsentTemplateQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/consenttemplates/${getEncodedUri(consentTemplateId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the coupons of a customer.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getCustomerCoupons(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetCustomerCouponsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/customercoupons`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Claims a customer coupon.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function claimCustomerCoupon(string baseSiteId, string userId, SAPCustomerCouponOperationRequest payload, map<string|string[]> headers = {}, *ClaimCustomerCouponQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/customercoupons/claim`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Discards a customer coupon.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function discardCustomerCoupon(string baseSiteId, string userId, SAPCustomerCouponOperationRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/customercoupons/discard`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Subscribes to coupon notifications.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function subscribeNotificationsOfCustomerCoupon(string baseSiteId, string userId, SAPCustomerCouponOperationRequest payload, map<string|string[]> headers = {}, *SubscribeNotificationsOfCustomerCouponQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/customercoupons/subscribeNotifications`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Unsubscribes from coupon notifications.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function unsubscribeNotificationsOfCustomerCoupon(string baseSiteId, string userId, SAPCustomerCouponOperationRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/customercoupons/unsubscribeNotifications`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Claims and remembers a customer coupon.
    #
    # + baseSiteId - Base site identifier
    # + couponCode - Coupon code. A valid coupon code can be used to get a discount
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    # 
    # # Deprecated
    @deprecated
    remote isolated function doClaimCustomerCoupon(string baseSiteId, string couponCode, string userId, map<string|string[]> headers = {}, *DoClaimCustomerCouponQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/customercoupons/${getEncodedUri(couponCode)}/claim`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Disclaims a customer coupon.
    #
    # + baseSiteId - Base site identifier
    # + couponCode - Coupon code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    # 
    # # Deprecated
    @deprecated
    remote isolated function unassignCustomerCoupon(string baseSiteId, string couponCode, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/customercoupons/${getEncodedUri(couponCode)}/claim`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Creates a coupon notification.
    #
    # + baseSiteId - Base site identifier
    # + couponCode - Coupon code. A valid coupon code can be used to get a discount
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    # 
    # # Deprecated
    @deprecated
    remote isolated function doSubscribeToCustomerCoupon(string baseSiteId, string couponCode, string userId, map<string|string[]> headers = {}, *DoSubscribeToCustomerCouponQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/customercoupons/${getEncodedUri(couponCode)}/notification`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes a coupon notification.
    #
    # + baseSiteId - Base site identifier
    # + couponCode - Coupon code. A valid coupon code can be used to get a discount
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    # 
    # # Deprecated
    @deprecated
    remote isolated function doUnsubscribeFromCustomerCoupon(string baseSiteId, string couponCode, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/customercoupons/${getEncodedUri(couponCode)}/notification`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves the customer groups.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getUserCustomerGroups(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetUserCustomerGroupsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/customergroups`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a list of entitlements
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - List of user's entitlements 
    remote isolated function getEntitlementInstances(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetEntitlementInstancesQueries queries) returns EntitlementInstanceList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/entitlementInstances`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves an entitlement
    #
    # + baseSiteId - Base site identifier
    # + entitlementInstanceId - Number of the entitlement instance
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Entitlement Details 
    remote isolated function getEntitlementInstance(string baseSiteId, string entitlementInstanceId, string userId, map<string|string[]> headers = {}, *GetEntitlementInstanceQueries queries) returns EntitlementInstance|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/entitlementInstances/${getEncodedUri(entitlementInstanceId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the future availability of the products.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getFutureStocksList(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetFutureStocksListQueries queries) returns ProductFutureStocksList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/futureStocks`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the future availability of a product.
    #
    # + baseSiteId - Base site identifier
    # + productCode - Product identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getFutureStocks(string baseSiteId, string productCode, string userId, map<string|string[]> headers = {}, *GetFutureStocksQueries queries) returns ProductFutureStocks|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/futureStocks/${getEncodedUri(productCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Changes customer's login name.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    # 
    # # Deprecated
    @deprecated
    remote isolated function replaceUserLogin(string baseSiteId, string userId, map<string|string[]> headers = {}, *ReplaceUserLoginQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/login`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Changes customer's login id.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function changeUserLoginId(string baseSiteId, string userId, ReplaceLoginIdInput payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/login`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Notify about successful login.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier
    # + headers - Headers to be sent with the request 
    # + return - Accepted 
    remote isolated function doPublishSuccessfulLoginEvent(string baseSiteId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/loginnotification`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets all notification preferences of the current customer
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getNotificationPreferences(string baseSiteId, string userId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/notificationpreferences`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the notification preferences of the current customer
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function updateNotificationPreferences(string baseSiteId, string userId, BasicnotificationPreferenceList payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/notificationpreferences`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Gets all site messages of current customer.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function siteMessages(string baseSiteId, string userId, map<string|string[]> headers = {}, *SiteMessagesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/notifications/sitemessages`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the order approval permissions.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrderApprovalPermissions(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetOrderApprovalPermissionsQueries queries) returns OrderApprovalPermissionList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orderApprovalPermissions`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an order approval permission.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createOrderApprovalPermission(string baseSiteId, string userId, OrderApprovalPermission payload, map<string|string[]> headers = {}, *CreateOrderApprovalPermissionQueries queries) returns OrderApprovalPermission|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orderApprovalPermissions`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the order approval permission.
    #
    # + baseSiteId - Base site identifier
    # + orderApprovalPermissionCode - Order approval permission code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrderApprovalPermission(string baseSiteId, string orderApprovalPermissionCode, string userId, map<string|string[]> headers = {}, *GetOrderApprovalPermissionQueries queries) returns OrderApprovalPermission|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orderApprovalPermissions/${getEncodedUri(orderApprovalPermissionCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the order approval permission.
    #
    # + baseSiteId - Base site identifier
    # + orderApprovalPermissionCode - Order approval permission code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function updateOrderApprovalPermission(string baseSiteId, string orderApprovalPermissionCode, string userId, OrderApprovalPermission payload, map<string|string[]> headers = {}, *UpdateOrderApprovalPermissionQueries queries) returns OrderApprovalPermission|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orderApprovalPermissions/${getEncodedUri(orderApprovalPermissionCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves the return request history of the customer.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getReturnRequests(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetReturnRequestsQueries queries) returns ReturnRequestList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orderReturns`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an order return request.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createReturnRequest(string baseSiteId, string userId, ReturnRequestEntryInputList payload, map<string|string[]> headers = {}, *CreateReturnRequestQueries queries) returns ReturnRequest|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orderReturns`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the details of a return request.
    #
    # + baseSiteId - Base site identifier
    # + returnRequestCode - Order returns request code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getReturnRequest(string baseSiteId, string returnRequestCode, string userId, map<string|string[]> headers = {}, *GetReturnRequestQueries queries) returns ReturnRequest|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orderReturns/${getEncodedUri(returnRequestCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the order return request using the specified code.
    #
    # + baseSiteId - Base site identifier
    # + returnRequestCode - Order returns request code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function updateReturnRequest(string baseSiteId, string returnRequestCode, string userId, ReturnRequestModification payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orderReturns/${getEncodedUri(returnRequestCode)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves the orders to approve.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrderApprovals(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetOrderApprovalsQueries queries) returns OrderApprovalList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orderapprovals`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves an order to approve.
    #
    # + baseSiteId - Base site identifier
    # + orderApprovalCode - Order approval code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrderApproval(string baseSiteId, string orderApprovalCode, string userId, map<string|string[]> headers = {}, *GetOrderApprovalQueries queries) returns OrderApproval|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orderapprovals/${getEncodedUri(orderApprovalCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a decision for the order approval.
    #
    # + baseSiteId - Base site identifier
    # + orderApprovalCode - Order approval code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function doMakeOrderApprovalDecision(string baseSiteId, string orderApprovalCode, string userId, OrderApprovalDecision payload, map<string|string[]> headers = {}, *DoMakeOrderApprovalDecisionQueries queries) returns OrderApprovalDecision|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orderapprovals/${getEncodedUri(orderApprovalCode)}/decision`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the order history of a customer.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getUserOrderHistory(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetUserOrderHistoryQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an order.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function placeOrder(string baseSiteId, string userId, map<string|string[]> headers = {}, *PlaceOrderQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the total number of orders for a customer.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function countUserOrders(string baseSiteId, string userId, map<string|string[]> headers = {}, *CountUserOrdersQueries queries) returns http:Response|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->head(resourcePath, headers);
    }

    # Place an order with authorized payment.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function paymentAuthorizedOrderPlacement(string baseSiteId, string userId, map<string|string[]> headers = {}, *PaymentAuthorizedOrderPlacementQueries queries) returns Order|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/paymentAuthorizedOrderPlacement`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the order.
    #
    # + baseSiteId - Base site identifier
    # + code - Order GUID or order code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getUserOrders(string baseSiteId, string code, string userId, map<string|string[]> headers = {}, *GetUserOrdersQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/${getEncodedUri(code)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get attachments for an order.
    #
    # + baseSiteId - Base site identifier
    # + code - Order Code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - List of attachments of an order 
    remote isolated function getUserOrderAttachments(string baseSiteId, string code, string userId, map<string|string[]> headers = {}, *GetUserOrderAttachmentsQueries queries) returns SAPOrderAttachmentList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/${getEncodedUri(code)}/attachments`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get binary attachment of an order
    #
    # + attachmentId - Attachment ID
    # + baseSiteId - Base site identifier
    # + code - Order Code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getUserOrderAttachmentBinary(string attachmentId, string baseSiteId, string code, string userId, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/${getEncodedUri(code)}/attachments/${getEncodedUri(attachmentId)}/download`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the billing document of an order. 
    #
    # + baseSiteId - Base site identifier
    # + billingDocId - Billing document identifier
    # + code - Order code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Returns PDF 
    remote isolated function getBillingDocument(string baseSiteId, string billingDocId, string code, string userId, map<string|string[]> headers = {}, *GetBillingDocumentQueries queries) returns http:Response|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/${getEncodedUri(code)}/billingDocuments/${getEncodedUri(billingDocId)}.pdf`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Cancels an order.
    #
    # + baseSiteId - Base site identifier
    # + code - Order code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function doCancelOrder(string baseSiteId, string code, string userId, CancellationRequestEntryInputList payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/${getEncodedUri(code)}/cancellation`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get invoices for an order.
    #
    # + baseSiteId - Base site identifier
    # + code - Order Code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - List of invoice of an order 
    remote isolated function getUserOrderInvoices(string baseSiteId, string code, string userId, map<string|string[]> headers = {}, *GetUserOrderInvoicesQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/${getEncodedUri(code)}/invoices`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get binary invoice of an order
    #
    # + baseSiteId - Base site identifier
    # + code - Order Code
    # + invoiceId - Invoice ID
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getUserOrderInvoiceBinary(string baseSiteId, string code, string invoiceId, string userId, map<string|string[]> headers = {}, *GetUserOrderInvoiceBinaryQueries queries) returns byte[]|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/${getEncodedUri(code)}/invoices/${getEncodedUri(invoiceId)}/download`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Cancels a service order
    #
    # + baseSiteId - Base site identifier
    # + code - Order code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function cancelServiceOrder(string baseSiteId, string code, string userId, CancellationRequestEntryInputList payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/${getEncodedUri(code)}/serviceOrder/cancellation`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Updates the scheduled service order date for the products in an order.
    #
    # + baseSiteId - Base site identifier
    # + code - Order code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function updateOrderServiceScheduleSlot(string baseSiteId, string code, string userId, ServiceScheduleSlot payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/${getEncodedUri(code)}/serviceOrder/serviceScheduleSlot`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves consignment tracking information
    #
    # + baseSiteId - Base site identifier
    # + consignmentCode - Consignment identifier
    # + orderCode - Order identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getConsignmentTrackingData(string baseSiteId, string consignmentCode, string orderCode, string userId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/${getEncodedUri(orderCode)}/consignments/${getEncodedUri(consignmentCode)}/tracking`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets a product configuration overview of an order entry
    #
    # + baseSiteId - Base site identifier
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + orderId - Order identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getConfigurationOverviewForOrderEntry(string baseSiteId, int:Signed32 entryNumber, string orderId, string userId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/${getEncodedUri(orderId)}/entries/${getEncodedUri(entryNumber)}/ccpconfigurator/configurationOverview`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Get textfield configuration for order entry.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + orderId - Order identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getTextfieldConfigurationForOrderEntry(string baseSiteId, string userId, int:Signed32 entryNumber, string orderId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/${getEncodedUri(orderId)}/entries/${getEncodedUri(entryNumber)}/configurator/textfield`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the CPQ configuration of an order entry.
    #
    # + baseSiteId - Base site identifier
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order
    # + orderId - The order identifier. Each order has a unique identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function readCpqConfigurationForOrderEntry(string baseSiteId, int:Signed32 entryNumber, string orderId, string userId, map<string|string[]> headers = {}) returns SAPCPQConfiguration|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orders/${getEncodedUri(orderId)}/entries/${getEncodedUri(entryNumber)}/cpqconfigurator/configuration`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the list of organizational customers.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgCustomers(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetOrgCustomersQueries queries) returns OrgUnitUserList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an organizational customer profile.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createOrgCustomer(string baseSiteId, string userId, OrgCustomerCreation payload, map<string|string[]> headers = {}, *CreateOrgCustomerQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the profile of the organizational unit customer.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer GUID
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgCustomer(string baseSiteId, string orgCustomerId, string userId, map<string|string[]> headers = {}, *GetOrgCustomerQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers/${getEncodedUri(orgCustomerId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the profile of the organizational unit customer.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer GUID
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function updateOrgCustomer(string baseSiteId, string orgCustomerId, string userId, OrgCustomerModification payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers/${getEncodedUri(orgCustomerId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves the approvers.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer GUID
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgCustomerApprovers(string baseSiteId, string orgCustomerId, string userId, map<string|string[]> headers = {}, *GetOrgCustomerApproversQueries queries) returns OrgUnitUserList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers/${getEncodedUri(orgCustomerId)}/approvers`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds an approver for an organizational customer.
    #
    # + approverId - Approver GUID
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer GUID
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function doAddApproverToOrgCustomer(string approverId, string baseSiteId, string orgCustomerId, string userId, map<string|string[]> headers = {}, *DoAddApproverToOrgCustomerQueries queries) returns B2BSelectionData|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers/${getEncodedUri(orgCustomerId)}/approvers/${getEncodedUri(approverId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes an approver for an organizational customer.
    #
    # + approverId - Approver GUID
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer GUID
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function removeApproverFromOrgCustomer(string approverId, string baseSiteId, string orgCustomerId, string userId, map<string|string[]> headers = {}, *RemoveApproverFromOrgCustomerQueries queries) returns B2BSelectionData|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers/${getEncodedUri(orgCustomerId)}/approvers/${getEncodedUri(approverId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves the organizational user groups.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer GUID
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgCustomerOrgUserGroups(string baseSiteId, string orgCustomerId, string userId, map<string|string[]> headers = {}, *GetOrgCustomerOrgUserGroupsQueries queries) returns OrgUnitUserGroupList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers/${getEncodedUri(orgCustomerId)}/orgUserGroups`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an Organizational organizational user group  objfor an organizational customer.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer GUID
    # + userGroupId - Organizational user group GUID
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function doAddOrgUserGroupToOrgCustomer(string baseSiteId, string orgCustomerId, string userGroupId, string userId, map<string|string[]> headers = {}, *DoAddOrgUserGroupToOrgCustomerQueries queries) returns B2BSelectionData|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers/${getEncodedUri(orgCustomerId)}/orgUserGroups/${getEncodedUri(userGroupId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes an organizational user group for an organizational customer.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer GUID
    # + userGroupId - Organizational user group GUID
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeOrgUserGroupFromOrgCustomer(string baseSiteId, string orgCustomerId, string userGroupId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers/${getEncodedUri(orgCustomerId)}/orgUserGroups/${getEncodedUri(userGroupId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves the permissions of the customer.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer GUID
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgCustomerPermissions(string baseSiteId, string orgCustomerId, string userId, map<string|string[]> headers = {}, *GetOrgCustomerPermissionsQueries queries) returns OrderApprovalPermissionList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers/${getEncodedUri(orgCustomerId)}/permissions`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a permission for an organizational customer.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer GUID
    # + permissionId - Permission GUID
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function doAddPermissionToOrgCustomer(string baseSiteId, string orgCustomerId, string permissionId, string userId, map<string|string[]> headers = {}, *DoAddPermissionToOrgCustomerQueries queries) returns B2BSelectionData|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers/${getEncodedUri(orgCustomerId)}/permissions/${getEncodedUri(permissionId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes a permission for an organizational customer.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer GUID
    # + permissionId - Permission GUID
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function removePermissionFromOrgCustomer(string baseSiteId, string orgCustomerId, string permissionId, string userId, map<string|string[]> headers = {}, *RemovePermissionFromOrgCustomerQueries queries) returns B2BSelectionData|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers/${getEncodedUri(orgCustomerId)}/permissions/${getEncodedUri(permissionId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Creates a role for an organizational customer.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer to which the role will be added
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function doAddRoleToOrgCustomer(string baseSiteId, string orgCustomerId, string userId, map<string|string[]> headers = {}, *DoAddRoleToOrgCustomerQueries queries) returns B2BSelectionData|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers/${getEncodedUri(orgCustomerId)}/roles`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the role from the organizational customer.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer from which the role will be removed
    # + roleId - Role that is removed from the user. Example roles: b2badmingroup, b2bmanagergroup, or b2bcustomergroup
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function removeRoleFromOrgCustomer(string baseSiteId, string orgCustomerId, string roleId, string userId, map<string|string[]> headers = {}, *RemoveRoleFromOrgCustomerQueries queries) returns B2BSelectionData|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgCustomers/${getEncodedUri(orgCustomerId)}/roles/${getEncodedUri(roleId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves the list of user groups in an organizational unit.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgUnitUserGroups(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetOrgUnitUserGroupsQueries queries) returns OrgUnitUserGroupList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitUserGroups`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a new user group in an organizational unit.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createOrgUnitUserGroup(string baseSiteId, string userId, OrgUnitUserGroup payload, map<string|string[]> headers = {}, *CreateOrgUnitUserGroupQueries queries) returns OrgUnitUserGroup|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitUserGroups`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the details of the organizational unit user group.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitUserGroupId - Organizational unit user group identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgUnitUserGroup(string baseSiteId, string orgUnitUserGroupId, string userId, map<string|string[]> headers = {}, *GetOrgUnitUserGroupQueries queries) returns OrgUnitUserGroup|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitUserGroups/${getEncodedUri(orgUnitUserGroupId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes the organizational unit user group.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitUserGroupId - Organizational unit user group identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeOrgUnitUserGroup(string baseSiteId, string orgUnitUserGroupId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitUserGroups/${getEncodedUri(orgUnitUserGroupId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates the organizational unit user group.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitUserGroupId - Organizational unit user group identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function updateOrgUnitUserGroup(string baseSiteId, string orgUnitUserGroupId, string userId, OrgUnitUserGroup payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitUserGroups/${getEncodedUri(orgUnitUserGroupId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves the order approval permissions.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitUserGroupId - Organizational unit user group identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrderApprovalPermissionsForOrgUnitUserGroup(string baseSiteId, string orgUnitUserGroupId, string userId, map<string|string[]> headers = {}, *GetOrderApprovalPermissionsForOrgUnitUserGroupQueries queries) returns OrderApprovalPermissionList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitUserGroups/${getEncodedUri(orgUnitUserGroupId)}/availableOrderApprovalPermissions`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the organizational customers.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitUserGroupId - Organizational unit user group identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getAvailableOrgCustomersForUserGroup(string baseSiteId, string orgUnitUserGroupId, string userId, map<string|string[]> headers = {}, *GetAvailableOrgCustomersForUserGroupQueries queries) returns OrgUnitUserList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitUserGroups/${getEncodedUri(orgUnitUserGroupId)}/availableOrgCustomers`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds an organizational customer as a member of an organizational unit user group.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitUserGroupId - Organizational unit user group identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function doAddOrgCustomerToOrgUnitUserGroupMembers(string baseSiteId, string orgUnitUserGroupId, string userId, map<string|string[]> headers = {}, *DoAddOrgCustomerToOrgUnitUserGroupMembersQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitUserGroups/${getEncodedUri(orgUnitUserGroupId)}/members`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes all organizational customers as user group members in the organizational unit.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitUserGroupId - Organizational unit user group identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeOrgUnitUserGroupMembers(string baseSiteId, string orgUnitUserGroupId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitUserGroups/${getEncodedUri(orgUnitUserGroupId)}/members`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Deletes an organizational customer from the organizational unit group members.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer from which the role will be removed
    # + orgUnitUserGroupId - Organizational unit user group identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeOrgCustomerFromOrgUnitUserGroupMembers(string baseSiteId, string orgCustomerId, string orgUnitUserGroupId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitUserGroups/${getEncodedUri(orgUnitUserGroupId)}/members/${getEncodedUri(orgCustomerId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Creates an order approval permission to a user group in the organizational unit.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitUserGroupId - Organizational unit user group to which the order approval permission will be added
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function doAddOrderApprovalPermissionToOrgUnitUserGroup(string baseSiteId, string orgUnitUserGroupId, string userId, map<string|string[]> headers = {}, *DoAddOrderApprovalPermissionToOrgUnitUserGroupQueries queries) returns B2BSelectionData|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitUserGroups/${getEncodedUri(orgUnitUserGroupId)}/orderApprovalPermissions`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the order approval permission from the organizational unit user group.
    #
    # + baseSiteId - Base site identifier
    # + orderApprovalPermissionCode - Order approval permission that will be removed from the organizational unit user group
    # + orgUnitUserGroupId - Organizational unit user group identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function removeOrderApprovalPermissionFromOrgUnitUserGroup(string baseSiteId, string orderApprovalPermissionCode, string orgUnitUserGroupId, string userId, map<string|string[]> headers = {}, *RemoveOrderApprovalPermissionFromOrgUnitUserGroupQueries queries) returns B2BSelectionData|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitUserGroups/${getEncodedUri(orgUnitUserGroupId)}/orderApprovalPermissions/${getEncodedUri(orderApprovalPermissionCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Creates a new organizational unit.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createOrgUnit(string baseSiteId, string userId, B2BUnit payload, map<string|string[]> headers = {}, *CreateOrgUnitQueries queries) returns B2BUnit|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the organizational unit.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitId - Organizational unit identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgUnit(string baseSiteId, string orgUnitId, string userId, map<string|string[]> headers = {}, *GetOrgUnitQueries queries) returns B2BUnit|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits/${getEncodedUri(orgUnitId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the organizational unit.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitId - Organizational unit identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function updateOrgUnit(string baseSiteId, string orgUnitId, string userId, B2BUnit payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits/${getEncodedUri(orgUnitId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves the account summary.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitId - Organizational unit identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getAccountSummary(string baseSiteId, string orgUnitId, string userId, map<string|string[]> headers = {}) returns AccountSummary|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits/${getEncodedUri(orgUnitId)}/accountSummary`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the addresses of an organizational unit.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitId - Organizational unit identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgUnitAddresses(string baseSiteId, string orgUnitId, string userId, map<string|string[]> headers = {}, *GetOrgUnitAddressesQueries queries) returns AddressList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits/${getEncodedUri(orgUnitId)}/addresses`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an address for the organizational unit.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitId - Organizational unit identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createOrgUnitAddress(string baseSiteId, string orgUnitId, string userId, Address payload, map<string|string[]> headers = {}, *CreateOrgUnitAddressQueries queries) returns Address|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits/${getEncodedUri(orgUnitId)}/addresses`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the organizational unit address.
    #
    # + addressId - Address identifier
    # + baseSiteId - Base site identifier
    # + orgUnitId - Organizational unit identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeOrgUnitAddress(string addressId, string baseSiteId, string orgUnitId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits/${getEncodedUri(orgUnitId)}/addresses/${getEncodedUri(addressId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates the organizational unit address.
    #
    # + addressId - Address identifier
    # + baseSiteId - Base site identifier
    # + orgUnitId - Organizational unit identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function updateOrgUnitAddress(string addressId, string baseSiteId, string orgUnitId, string userId, Address payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits/${getEncodedUri(orgUnitId)}/addresses/${getEncodedUri(addressId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves the available parent units.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitId - Organizational unit identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getAvailableParentUnits(string baseSiteId, string orgUnitId, string userId, map<string|string[]> headers = {}, *GetAvailableParentUnitsQueries queries) returns B2BUnitNodeList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits/${getEncodedUri(orgUnitId)}/availableParents`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the users who belong to the organizational unit.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitId - Organizational unit identifier
    # + roleId - Role that is returned. Example roles: b2bapprovergroup, b2badmingroup, b2bmanagergroup, or b2bcustomergroup
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgUnitUsers(string baseSiteId, string orgUnitId, string roleId, string userId, map<string|string[]> headers = {}, *GetOrgUnitUsersQueries queries) returns OrgUnitUserList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits/${getEncodedUri(orgUnitId)}/availableUsers/${getEncodedUri(roleId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds an organizational unit dependent role to a specific organizational customer.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer to which the role will be added
    # + orgUnitId - Organizational unit identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function doAddOrgUnitRoleToOrgCustomer(string baseSiteId, string orgCustomerId, string orgUnitId, string userId, map<string|string[]> headers = {}, *DoAddOrgUnitRoleToOrgCustomerQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits/${getEncodedUri(orgUnitId)}/orgCustomers/${getEncodedUri(orgCustomerId)}/roles`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes an organizational, unit-dependent role from an organizational customer.
    #
    # + baseSiteId - Base site identifier
    # + orgCustomerId - Organizational customer from which the role will be removed
    # + orgUnitId - Organizational unit identifier
    # + roleId - Role that is removed from the user. Example roles: b2badmingroup, b2bmanagergroup, or b2bcustomergroup
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removeOrgUnitRoleFromOrgCustomer(string baseSiteId, string orgCustomerId, string orgUnitId, string roleId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits/${getEncodedUri(orgUnitId)}/orgCustomers/${getEncodedUri(orgCustomerId)}/roles/${getEncodedUri(roleId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves the financial organizational documents.
    #
    # + baseSiteId - Base site identifier
    # + orgUnitId - Organizational unit identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgDocuments(string baseSiteId, string orgUnitId, string userId, map<string|string[]> headers = {}, *GetOrgDocumentsQueries queries) returns OrgDocumentList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits/${getEncodedUri(orgUnitId)}/orgDocuments`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the attachment of a document.
    #
    # + baseSiteId - Base site identifier
    # + orgDocumentAttachmentId - Organizational document attachment identifier
    # + orgDocumentId - Organizational document identifier
    # + orgUnitId - Organizational unit identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getOrgDocumentAttachment(string baseSiteId, string orgDocumentAttachmentId, string orgDocumentId, string orgUnitId, string userId, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnits/${getEncodedUri(orgUnitId)}/orgDocuments/${getEncodedUri(orgDocumentId)}/attachments/${getEncodedUri(orgDocumentAttachmentId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the available business approval processes.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgUnitsAvailableApprovalProcesses(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetOrgUnitsAvailableApprovalProcessesQueries queries) returns B2BApprovalProcessList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitsAvailableApprovalProcesses`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the root organizational unit node.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getOrgUnitsRootNodeTree(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetOrgUnitsRootNodeTreeQueries queries) returns B2BUnitNode|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/orgUnitsRootNodeTree`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the password of a customer.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Accepted 
    # 
    # # Deprecated
    @deprecated
    remote isolated function replaceUserPassword(string baseSiteId, string userId, map<string|string[]> headers = {}, *ReplaceUserPasswordQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/password`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Updates the password of a customer.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function changeUserPassword(string baseSiteId, string userId, ReplacePasswordInput payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/password`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves all credit card payment details of the customer.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getPaymentDetailsList(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetPaymentDetailsListQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/paymentdetails`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves detailed information for a specific credit card payment.
    #
    # + baseSiteId - Base site identifier
    # + paymentDetailsId - Payment details identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getPaymentDetails(string baseSiteId, string paymentDetailsId, string userId, map<string|string[]> headers = {}, *GetPaymentDetailsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/paymentdetails/${getEncodedUri(paymentDetailsId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates existing detailed information for a specific credit card payment. 
    #
    # + baseSiteId - Base site identifier
    # + paymentDetailsId - Payment details identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function replacePaymentDetails(string baseSiteId, string paymentDetailsId, string userId, PaymentDetails payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/paymentdetails/${getEncodedUri(paymentDetailsId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes detailed information for a specific credit card payment.
    #
    # + baseSiteId - Base site identifier
    # + paymentDetailsId - Payment details identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function removePaymentDetails(string baseSiteId, string paymentDetailsId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/paymentdetails/${getEncodedUri(paymentDetailsId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates existing detailed information for a specific credit card payment. 
    #
    # + baseSiteId - Base site identifier
    # + paymentDetailsId - Payment details identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function updatePaymentDetails(string baseSiteId, string paymentDetailsId, string userId, PaymentDetails payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/paymentdetails/${getEncodedUri(paymentDetailsId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        xml? xmlBody = check xmldata:fromJson(jsonBody);
        request.setPayload(xmlBody, "application/xml");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves the product interests of a customer.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getProductInterests(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetProductInterestsQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/productinterests`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates product interests.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function createProductInterest(string baseSiteId, string userId, map<string|string[]> headers = {}, *CreateProductInterestQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/productinterests`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the product interests.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function removeProductInterest(string baseSiteId, string userId, map<string|string[]> headers = {}, *RemoveProductInterestQueries queries) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/productinterests`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves all quotes for a customer.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getQuotes(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetQuotesQueries queries) returns QuoteList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/quotes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a quote.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createQuote(string baseSiteId, string userId, QuoteStarter payload, map<string|string[]> headers = {}, *CreateQuoteQueries queries) returns Quote|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/quotes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the quote.
    #
    # + baseSiteId - Base site identifier
    # + quoteCode - Identifying code of the quote
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getQuote(string baseSiteId, string quoteCode, string userId, map<string|string[]> headers = {}, *GetQuoteQueries queries) returns Quote|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/quotes/${getEncodedUri(quoteCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the quote.
    #
    # + baseSiteId - Base site identifier
    # + quoteCode - Identifying code of the quote
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function replaceQuote(string baseSiteId, string quoteCode, string userId, QuoteMetadata payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/quotes/${getEncodedUri(quoteCode)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Updates the quote.
    #
    # + baseSiteId - Base site identifier
    # + quoteCode - Identifying code of the quote
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function updateQuote(string baseSiteId, string quoteCode, string userId, QuoteMetadata payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/quotes/${getEncodedUri(quoteCode)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Creates workflow actions for the quote.
    #
    # + baseSiteId - Base site identifier
    # + quoteCode - Code of the quote
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function performQuoteAction(string baseSiteId, string quoteCode, string userId, QuoteAction payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/quotes/${getEncodedUri(quoteCode)}/action`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the quote attachment.
    #
    # + attachmentId - Identifying code of the quote attachment
    # + baseSiteId - Base site identifier
    # + quoteCode - Identifying code of the quote
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getQuoteAttachment(string attachmentId, string baseSiteId, string quoteCode, string userId, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/quotes/${getEncodedUri(quoteCode)}/attachments/${getEncodedUri(attachmentId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a comment for a quote.
    #
    # + baseSiteId - Base site identifier
    # + quoteCode - Identifying code of the quote
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createCommentForQuote(string baseSiteId, string quoteCode, string userId, CreateComment payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/quotes/${getEncodedUri(quoteCode)}/comments`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Creates a discount for an existing quote.
    #
    # + baseSiteId - Base site identifier
    # + quoteCode - Code of the quote
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createQuoteDiscount(string baseSiteId, string quoteCode, string userId, QuoteDiscount payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/quotes/${getEncodedUri(quoteCode)}/discounts`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Creates a comment for a quote entry.
    #
    # + baseSiteId - Base site identifier
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + quoteCode - Code of the quote
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createQuoteEntryComment(string baseSiteId, int entryNumber, string quoteCode, string userId, CreateComment payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/quotes/${getEncodedUri(quoteCode)}/entries/${getEncodedUri(entryNumber)}/comments`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets a product configuration overview of an quote entry
    #
    # + baseSiteId - Base site identifier
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + quoteId - Quote identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getConfigurationOverviewForQuote(string baseSiteId, int:Signed32 entryNumber, string quoteId, string userId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/quotes/${getEncodedUri(quoteId)}/entries/${getEncodedUri(entryNumber)}/ccpconfigurator/configurationOverview`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Get textfield configuration for quote entry.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + quoteId - Quote identifier
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getTextfieldConfigurationForQuoteEntry(string baseSiteId, string userId, int:Signed32 entryNumber, string quoteId, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/quotes/${getEncodedUri(quoteId)}/entries/${getEncodedUri(entryNumber)}/configurator/textfield`;
        return self.clientEp->get(resourcePath, headers);
    }

    #  Retrieves the CPQ configuration of a quote entry.
    #
    # + baseSiteId - Base site identifier
    # + entryNumber - The entry number. Each entry in a quote has an entry number. Quote entries are numbered in ascending order
    # + quoteId - The quote identifier. Each quote has a unique identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function readCpqConfigurationForQuoteEntry(string baseSiteId, int:Signed32 entryNumber, string quoteId, string userId, map<string|string[]> headers = {}) returns SAPCPQConfiguration|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/quotes/${getEncodedUri(quoteId)}/entries/${getEncodedUri(entryNumber)}/cpqconfigurator/configuration`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the replenishment orders.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getReplenishmentOrders(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetReplenishmentOrdersQueries queries) returns ReplenishmentOrderList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/replenishmentOrders`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the replenishment order.
    #
    # + baseSiteId - Base site identifier
    # + replenishmentOrderCode - Replenishment order code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getReplenishmentOrder(string baseSiteId, string replenishmentOrderCode, string userId, map<string|string[]> headers = {}, *GetReplenishmentOrderQueries queries) returns ReplenishmentOrder|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/replenishmentOrders/${getEncodedUri(replenishmentOrderCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the replenishment order.
    #
    # + baseSiteId - Base site identifier
    # + replenishmentOrderCode - Unique code for the replenishment order
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function updateReplenishmentOrder(string baseSiteId, string replenishmentOrderCode, string userId, map<string|string[]> headers = {}, *UpdateReplenishmentOrderQueries queries) returns ReplenishmentOrder|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/replenishmentOrders/${getEncodedUri(replenishmentOrderCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Retrieves the history of the replenishment order.
    #
    # + baseSiteId - Base site identifier
    # + replenishmentOrderCode - Replenishment order code
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getReplenishmentOrderHistory(string baseSiteId, string replenishmentOrderCode, string userId, map<string|string[]> headers = {}, *GetReplenishmentOrderHistoryQueries queries) returns OrderHistoryList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/replenishmentOrders/${getEncodedUri(replenishmentOrderCode)}/orders`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get textfield configuration for saved cart entry
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + cartId - Cart identifier
    # + entryNumber - Each entry in a cart has an entry number. Cart entries are numbered in ascending order, starting with zero
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getTextfieldConfigurationForSavedCartEntry(string baseSiteId, string userId, string cartId, int:Signed32 entryNumber, map<string|string[]> headers = {}) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/savedCarts/${getEncodedUri(cartId)}/entries/${getEncodedUri(entryNumber)}/configurator/textfield`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the CPQ configuration of a saved cart entry.
    #
    # + baseSiteId - Base site identifier
    # + entryNumber - Each entry in a cart has an entry number. Saved cart entries are numbered in ascending order
    # + savedCartId - The saved cart identifier. Each saved cart has a unique identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function readCpqConfigurationForSavedCartEntry(string baseSiteId, int:Signed32 entryNumber, string savedCartId, string userId, map<string|string[]> headers = {}) returns SAPCPQConfiguration|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/savedCarts/${getEncodedUri(savedCartId)}/entries/${getEncodedUri(entryNumber)}/cpqconfigurator/configuration`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the object associated with a ticket.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getTicketAssociatedObjects(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetTicketAssociatedObjectsQueries queries) returns TicketAssociatedObjectList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/ticketAssociatedObjects`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the tickets of a customer.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getTickets(string baseSiteId, string userId, map<string|string[]> headers = {}, *GetTicketsQueries queries) returns TicketList|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/tickets`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a ticket.
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createTicket(string baseSiteId, string userId, TicketStarter payload, map<string|string[]> headers = {}, *CreateTicketQueries queries) returns Ticket|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/tickets`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves a ticket.
    #
    # + baseSiteId - Base site identifier
    # + ticketId - Ticket identifier.<br> Example: ```00000001```
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getTicket(string baseSiteId, string ticketId, string userId, map<string|string[]> headers = {}, *GetTicketQueries queries) returns Ticket|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/tickets/${getEncodedUri(ticketId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a ticket event.
    #
    # + baseSiteId - Base site identifier
    # + ticketId - Ticket identifier.<br> Example: ```00000001```
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createTicketEvent(string baseSiteId, string ticketId, string userId, TicketEvent payload, map<string|string[]> headers = {}, *CreateTicketEventQueries queries) returns TicketEvent|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/tickets/${getEncodedUri(ticketId)}/events`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Creates an attachment for an event in a ticket.
    #
    # + baseSiteId - Base site identifier
    # + eventCode - Ticket event code is specific to each event. It is used to identify an event.<br>Example: ```00000A15```
    # + ticketId - Ticket identifier.<br> Example: ```00000001```
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Created 
    remote isolated function createTicketEventAttachment(string baseSiteId, string eventCode, string ticketId, string userId, EventCodeAttachmentsBody payload, map<string|string[]> headers = {}, *CreateTicketEventAttachmentQueries queries) returns TicketEventAttachment|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/tickets/${getEncodedUri(ticketId)}/events/${getEncodedUri(eventCode)}/attachments`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves an attachment for an event in a ticket.
    #
    # + attachmentId - Attachment identifier.<br>Example: ```001```
    # + baseSiteId - Base site identifier
    # + eventCode - Ticket event code that is specific to each event. It is used to identify an event.<br>Example: ```00000A15```
    # + ticketId - Ticket identifier.<br> Example: ```00000001```
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - OK 
    remote isolated function getTicketEventAttachment(string attachmentId, string baseSiteId, string eventCode, string ticketId, string userId, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/tickets/${getEncodedUri(ticketId)}/events/${getEncodedUri(eventCode)}/attachments/${getEncodedUri(attachmentId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates verification token
    #
    # + baseSiteId - Base site identifier
    # + userId - User identifier or one of the literals : 'current' for currently authenticated user, 'anonymous' for anonymous user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createVerificationToken(string baseSiteId, string userId, CreateVerificationTokenInput payload, map<string|string[]> headers = {}) returns VerificationToken|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/users/${getEncodedUri(userId)}/verificationToken`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the voucher by voucher code.
    #
    # + baseSiteId - Base site identifier
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    remote isolated function getVoucherByCode(string baseSiteId, SAPVoucherOperationRequest payload, map<string|string[]> headers = {}, *GetVoucherByCodeQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/vouchers/code/search`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the voucher.
    #
    # + baseSiteId - Base site identifier
    # + code - Voucher identifier (code)
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - OK 
    # 
    # # Deprecated
    @deprecated
    remote isolated function getVoucher(string baseSiteId, string code, map<string|string[]> headers = {}, *GetVoucherQueries queries) returns xml|error {
        string resourcePath = string `/${getEncodedUri(baseSiteId)}/vouchers/${getEncodedUri(code)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }
}
